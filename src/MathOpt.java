public class MathOpt {
    public void math(){
        double x = 3.14;
        double y = -2.10;

//        Math.abs(y); value is always positive
//        Math.max(x,y);
//        Math.min(x,y);
//        Math.sqrt(x);
//        Math.round(y);
//        Math.ceil(x); always round up 4.0
//        Math.floor(x); always round down
        double z = Math.abs(y);

        System.out.println(z);
    }
}
